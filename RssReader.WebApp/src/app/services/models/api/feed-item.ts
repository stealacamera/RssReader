export interface FeedItem {
    feedName: string,
    feedIconUrl?: string,
    title?: string,
    author?: string,
    link?: string,
    description?: string,
    content?: string,
    publishedAt?: Date
}